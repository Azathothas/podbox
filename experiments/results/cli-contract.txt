== conditions
date              2026-09-21T05:41:07Z
host kernel       3.6.9-b4195d69.x86_64
podbox            podbox 0.1.0
engine            podman version 6.1.2
jq                jq-1.8.2

== 1. T-0801's own acceptance: the table is data
  exit              0
  rows              141
  of which verbs    53
  statuses          Degraded Native None Stub
  length >= 60 and every status one of the four: rc=0
  rows with no note 0

== 2. the table DECIDES, rather than describing what a parser does
  run -v        rc=125
    podbox run: -v, --volume is in the parity table with status None: podbox cannot mount(2) on this
  run --no-such-flag rc=125
    podbox run: unknown option "--no-such-flag". It has no row in the parity table; `podbox system i
  run -i (a Stub)   rc=0

== 3. every verb the table calls None says so with docker's 125
  restart  rc=125  message is the table's row: yes
  top      rc=125  message is the table's row: yes
  attach   rc=125  message is the table's row: yes
  pause    rc=125  message is the table's row: yes
  unpause  rc=125  message is the table's row: yes
  stats    rc=125  message is the table's row: yes

== 4. T-0803: podbox answers to `docker` and `podman` on PATH
  docker  stdout [hi] rc=0  banner says: invoked as `docker`
  podman  stdout [hi] rc=0  banner says: invoked as `podman`

== 5. T-0803's ruling: the docker name is refused where a daemon answers
  a docker daemon here: absent
  install-names     rc=0
  ⚠ the refusal half could not be measured on this machine: no daemon
  every installed name is a symlink: yes
