[package]
name = "userland-execve"
version = "0.2.0"
edition = "2021"
description = "An implementation of execve() in user space"
repository = "https://github.com/io12/userland-execve-rust"
readme = "README.md"
license = "MIT"

# ⛔ Patched out per TODO/deps.md T-0909 and TODO/packaging.md T-1003: `goblin`
# is refused by T-0908 (a program-header walk of a few dozen lines replaces
# it; `podbox-enter`'s `abi` module is that walk) and `nix` is replaced by the
# `sys` module (`crates/podbox-probe/src/sys.rs`), which already declares the
# trap the vendored sources need. The vendored sources still naming either are
# rewired at ladder time. This tree stays out of the workspace and uncompiled
# until then.
